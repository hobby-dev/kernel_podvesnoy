Plan: 
pointers, memory, 

3 вида памяти:
1) автоматическая (стэк)
    все локальные переменные
    передача параметров
    точки возврата
    очищается автоматически
2) статическая (при старте программы)
3) динамическая
    выделяется динамически
    не очищается автоматически
